<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_editor_host"
    app:startDestination="@id/editorFragment">
    <fragment
        android:id="@+id/editorFragment"
        android:name="com.app.spark.activity.custom_gallery.editor_fragment.EditorFragment"
        android:label="fragment_editor"
        tools:layout="@layout/fragment_editor" >
       <!-- <action
            android:id="@+id/action_editor_to_another"
            app:destination="@id/previewFragment"
            app:popUpTo="@+id/cameraFragment" >
        </action>-->
    </fragment>
  <!--  <fragment
        android:id="@+id/previewFragment"
        android:name="com.app.spark.activity.camera.fragment.PreviewFragment"
        android:label="fragment_preview"
        tools:layout="@layout/fragment_preview" />-->
</navigation>